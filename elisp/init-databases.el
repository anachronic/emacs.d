;;; init-databases.el --- Database handling.
;;; Commentary:
;;; Code:

;; EDBI is something i've wanted for a long time. I thought there was
;; no such thing, but i guess i was wrong. Nice
(use-package edbi
  :ensure t
  :defer t
  )

(provide 'init-databases)
;;; init-databases.el ends here
